# LangSmith (traceability)
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=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
LANGCHAIN_PROJECT=garv-amadeus-text2sql-poc

# LLM Provider
LLM_PROVIDER=gemini

# Ollama (kept for fallback)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:7b

# Gemini (Google AI Studio) ✅ FIXED
GEMINI_API_KEY=AIzaSyBelGcd_
GEMINI_MODEL=gemini-2.5-flash
GEMINI_TEMPERATURE=0.0

# MCP
USE_MCP=1
MCP_BASE_URL=http://127.0.0.1:9000
MCP_API_KEY=garv-secret

# --- Agentic / Supervisor mode ---
USE_SUPERVISOR=1            # 1=true, 0=false (fallback to sequential graph)
SUPERVISOR_MAX_STEPS=8      # safety cap to avoid infinite loops
INCLUDE_DEBUG=1             # return trace/debug in API response (UI trace panel)
ENABLE_VIZ=1                # allow backend to produce chart_spec automatically

# Optional: choose which orchestrator to run (future-proof)
ORCHESTRATION_MODE=supervisor   # "sequential" | "supervisor"
